import 'package:flutter/material.dart';

/// Outer padding of a card.
EdgeInsets get cardPadding => EdgeInsets.all(12.0);

/// Padding inserted below the header of a card.
Padding get headerSeparator => Padding(padding: EdgeInsets.only(top: 12.0));
